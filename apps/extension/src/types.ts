// Re-export shared types
export type { 
  WordEntry, 
  StorageState, 
  WordHistoryEntry,
  Achievement,
  WeeklyChallenge,
  ReminderSettings
} from '@daily-word/shared';

export { 
  DEFAULT_STORAGE_STATE,
  ALL_ACHIEVEMENTS,
  POINTS
} from '@daily-word/shared';
